{
  "name": "plainview",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["apps/*", "services/*", "packages/*", "integrations/*"],
  "scripts": {
    "dev": "npm run -ws dev",
    "build": "npm run -ws build",
    "test": "npm run -ws test",
    "typecheck": "npm run -ws typecheck"
  }
}
